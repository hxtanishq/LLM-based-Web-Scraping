this a llm based web scraping tool that allows to scrape data from urls and convert that into JSON file,

to run this script
first install the requirements
and run the code in terminal as
python scraper.py
